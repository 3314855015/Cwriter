plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace 'com.cwriter.export'
    compileSdk 36

    defaultConfig {
        minSdk 26  // 提高minSdk到26以支持POI库
        targetSdk 36
        versionCode 150
        versionName "1.1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    lint {
        abortOnError false
    }
    
    sourceSets {
        main {
            assets.srcDirs = ['assets']
        }
    }
    
    // 解决META-INF冲突，同时保留字体资源
    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/NOTICE.txt'
        pickFirst 'META-INF/gradle/incremental.annotation.processors'
        // 确保包含iText字体资源
        pickFirst '**/*.afm'
        pickFirst '**/*.ttf'
        pickFirst '**/*.otf'
    }
    
    // 强制包含依赖库到classes.jar中
    publishing {
        singleVariant('release') {
            withSourcesJar()
            withJavadocJar()
        }
    }
}

repositories {
    google()
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    // uni-app SDK - 需要从uni-app SDK中获取
    compileOnly fileTree(dir: 'libs', include: ['uniapp-v8-release.aar'])
    
    // FastJSON - uni-app使用
    compileOnly 'com.alibaba:fastjson:1.2.83'
    
    // AndroidX权限支持库（不在package.json中声明的依赖）
    implementation 'androidx.core:core:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    
    // 其他依赖由package.json统一管理，避免重复声明
    // 主要依赖（PDF/DOCX相关）已在package.json中声明
}

// 简化打包流程，避免依赖冲突
tasks.whenTaskAdded { task ->
    if (task.name == 'bundleReleaseAar') {
        task.doLast {
            println "🔧 AAR 构建完成，使用 api 依赖方式避免冲突"
            
            def aarFile = file("build/outputs/aar/export-native-release.aar")
            if (aarFile.exists()) {
                println "✅ AAR 文件位置: ${aarFile.absolutePath}"
                println "💡 提示：依赖通过 api 方式传递，主项目会自动解析"
            }
        }
    }
}


